describe('create-user-service', () => {
  beforeEach(() => {})

  it.todo(
    'should not be able to create a new user with an email that is already registered',
  )
  it.todo('should be able to create a new user with success')
})
